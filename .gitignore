# ============================================================================
# ГЛАВНЫЙ .gitignore — ПРАВИЛА ДЛЯ ВСЕГО ПРОЕКТА
# ============================================================================

# --- ЗАВИСИМОСТИ И РЕЗУЛЬТАТЫ СБОРКИ ---
# Игнорируем папки с пакетами - самую "тяжелую" часть проекта.
node_modules/

# Игнорируем результаты сборки. Они генерируются заново на каждом компьютере.
dist/

# Игнорируем кэш Turborepo и PNPM.
.turbo/
.pnpm-store/
.eslintcache

# Игнорируем скомпилированные TypeScript-файлы (для скриптов).
scripts/*.js
scripts/*.d.ts

# Игнорируем файлы с информацией о сборке TypeScript.
*.tsbuildinfo


# --- ЛОГИ И ВРЕМЕННЫЕ ФАЙЛЫ ---
# Игнорируем папки с логами и любые файлы с расширением .log.
logs
*.log

# Игнорируем отладочные логи от разных пакетных менеджеров.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
server.log


# --- ФАЙЛЫ ОПЕРАЦИОННОЙ СИСТЕМЫ И РЕДАКТОРОВ ---
# Системный "мусор" от macOS и Windows.
.DS_Store
Thumbs.db

# Настройки для редакторов кода (глобальные).
.vscode/
.idea/
.continue/


# --- СЕКРЕТЫ И КОНФИДЕНЦИАЛЬНЫЕ ДАННЫЕ (ОЧЕНЬ ВАЖНО!) ---
# Никогда не добавляем в Git файлы окружения, так как в них хранятся
# пароли, ключи API и другие секреты.
.env
.env.*


# --- СПЕЦИФИЧНЫЕ ДЛЯ ПРОЕКТА ИСКЛЮЧЕНИЯ ---
# ❗️ КРИТИЧЕСКИ ВАЖНО: Исключаем попадание в Git локальной базы данных.
#    Это предотвращает конфликты и утечку данных.
backend/db/*.json

# ❗️ КРИТИЧЕСКИ ВАЖНО: Исключаем папку с файлами, загруженными пользователями.
backend/uploads/