// ============================================================================
// src/main.tsx - ТОЧКА ВХОДА В ПРИЛОЖЕНИЕ ("КЛЮЧ ЗАЖИГАНИЯ")
// ============================================================================
// Этот файл - самый первый, который запускается при открытии админки
// в браузере. Его главная задача - найти "место" в HTML-файле
// и "вмонтировать" в него всё наше React-приложение.
// ============================================================================

// --- 1. ИМПОРТЫ ---
// Подключаем все необходимые "детали" и "инструменты".

import { StrictMode } from "react";
// StrictMode - это специальный "помощник" от React. Он не меняет внешний вид
// приложения, но включает режим "диагностики", который выводит в консоль
// предупреждения о возможных проблемах в коде. Очень полезно для разработки.

import { createRoot } from "react-dom/client";
// createRoot - это новая, современная функция из React 18 для "запуска"
// нашего приложения. Она более производительная, чем старые методы.

import App from "./App.tsx";
// Импортируем наш самый главный, корневой компонент приложения.
// Можно представить, что App - это "кузов" автомобиля, на который
// мы будем навешивать все остальные детали (страницы, кнопки и т.д.).

/**
 * Важнейший импорт! Этим действием мы подключаем все наши глобальные стили.
 * Здесь объявляются CSS-переменные для цветов, подключаются шрифты
 * и задаются базовые стили для всего сайта. Без этого импорта
 * приложение будет "голым" и без стилей.
 */
import "./index.css";

// --- 2. ИНФОРМАЦИОННЫЙ БАННЕР ---
// Этот блок выводит красивый и полезный баннер в консоль разработчика
// в браузере. Это помогает сразу понять, какой проект запущен,
// особенно когда работаешь с несколькими проектами одновременно.

console.log("\n" + "=".repeat(60));
console.log("🧠 NABLUДATEL PLATFORM — ADMIN PANEL");
console.log(
  "🛠 Панель администратора для управления проектами, пользователями и контентом."
);
console.log("🖥 Основана на Vite + React + TypeScript.");
console.log("🔧 Подключение к backend через REST API.");
console.log(
  "✨ Часть масштабируемой платформы NABLUДATEL, разрабатываемой @intragentt."
);
console.log("📬 По вопросам и доступу: https://t.me/intragentt");
console.log("=".repeat(60) + "\n");

// --- 3. ЗАПУСК ПРИЛОЖЕНИЯ ---
// Это и есть тот самый "поворот ключа зажигания".

// Находим "якорь" в нашем главном index.html файле.
// Это обычный div с идентификатором (id) "root".
const rootElement = document.getElementById("root")!;

// Создаем "корень" нашего React-приложения в этом "якоре".
const root = createRoot(rootElement);

// Запускаем процесс "отрисовки" (рендеринга) нашего приложения.
root.render(
  // Оборачиваем всё в StrictMode, чтобы React помогал нам находить
  // потенциальные проблемы в коде во время разработки.
  <StrictMode>
    {/* Указываем, что именно нужно отрисовать - наш главный компонент App. */}
    <App />
  </StrictMode>
);
