// ============================================================================
// utils/paths.ts - "ОФИЦИАЛЬНАЯ КАРТА" ПРОЕКТА
// ============================================================================
// Этот файл - это централизованное хранилище важных путей в нашем проекте.
// Его задача - предоставить один-единственный, всегда правильный источник
// информации о том, где находятся ключевые папки.
// ============================================================================

import path from "path";

/**
 * Абсолютный путь к папке `uploads`.
 *
 * Мы используем `path.join` и `__dirname` для создания надежного пути,
 * который будет работать правильно, независимо от того, из какой директории
 * мы запускаем сервер.
 *
 * Если в будущем мы решим переместить или переименовать папку `uploads`,
 * нам нужно будет изменить путь только в ЭТОМ ОДНОМ МЕСТЕ.
 */
export const uploadsPath = path.join(__dirname, "..", "uploads");
